<section id="details-info">
    <h1>Details</h1>
    {{#with mainItem}}
    <div class="coin-image">
        <img src="{{image}}">
    </div>

    <div class="coin-info">
        <div class="coin-text">
            <h1 id="name">{{name}}</h1>
            <h3 id="payment">Payment method: {{payment}}</h3>
            <p id="price"><span>Price: ${{price}}</span></p>
            <p id="description">{{description}}.</p>
        </div>
        {{#if user}}
        <div class="product-btn">
            {{#if isOwner}}
            <!--Only for registered user and author of the publication -->
            <div class="author">
                <a href="/mainItem/{{_id}}/edit" class="btn-edit">Edit</a>
                <a href="/mainItem/{{_id}}/delete" class="btn-delete">Delete</a>
            </div>
            {{else}}
                {{#if isBooked}}
            <!-- Logged in user who has already buy this crypto-->
            <p class="buy-message">You already bought these crypto coins.</p>
                {{else}}
            <!-- Logged in user who has not yet buy this crypto-->
            <a href="/mainItem/{{_id}}/booked" class="btn-buy">Buy</a>
                {{/if}}
            {{/if}}
        </div>
        {{/if}}
    </div>
    {{/with}}
</section>

{{!-- <section class="details-page">
        <h1>Details</h1>
        <article class="details-card">
            {{#with mainItem}}
            <article class="details-card-text">
                <h2>Title:<br> {{title}}</h2>
                <h3>Author: {{author}}</h3>
                <h3>Stars: {{stars}}</h3>
                <h3>Genre: {{genre}}</h3>
                <h3>Review: {{review}}</h3>

                <!-- If there is no registered user, do not display buttons-->
                {{#if user}}
                <div class="buttons">
                    {{#if isOwner}}
                    <!-- Only for registered user and author of the review -->
                    <a href="/mainItem/{{_id}}/edit" class="btn-edit">Edit</a>
                    <a href="/mainItem/{{_id}}/delete" class="btn-delete">Delete</a>
                    {{else}}
                        {{#if isBooked}}
                    <!-- logged in user who has already wished book-->
                    <p class="wish-pub">You already added the book to your wish list</p>
                        {{else}}
                    <!-- logged in user who has not yet wished book-->
                    <a href="/mainItem/{{_id}}/booked" class="btn-wish">Wish to read</a>
                        {{/if}}
                    {{/if}}
                </div>
                {{/if}}
            </article>

            <article class="details-card-image">
                <img src="{{image}}">
            </article>
            {{/with}}
        </article>
    </section> --}}

{{!-- <section id="viewhotelDetails">
    <h2>Details</h2>
    {{#with mainItem}}
    <div class="hotel-ticket">
        <div class="hotel-left">
            <img src="{{imageUrl}}"alt="">
        </div>
        <div class="hotel-right">
            <div>
                <h3>{{name}}</h3>
            </div>
            <div>
                {{city}}
            </div>
            <p><span >Free rooms: {{rooms}}</span> </p>

            {{#if isOwner}}
            <a href="/mainItem/{{_id}}/edit" class="edit">Edit</a>
            <a href="/mainItem/{{_id}}/delete" class="remove">Delete</a>
            {{else}}
                {{#if isBooked}}
                <p><span class="green">You already have booked a room</span> </p>
                {{else}}
                <a href="/mainItem/{{_id}}/book" class="book">Book</a>
                {{/if}}
            {{/if}}

        </div>

    </div>
    {{/with}}
</section> --}}


