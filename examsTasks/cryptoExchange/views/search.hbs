
<section class="crypto-search">
    <h1>Search</h1>

    <form class="search-form"  method="get">
        <input type="text" class="search-coin" name="name" placeholder="Search here..." value="{{searchName}}">
        <select id="payment" name="payment">
            <option value="crypto-wallet">Crypto Wallet</option>
            <option value="credit-card">Credit Card</option>
            <option value="debit-card">Debit Card</option>
            <option value="paypal">PayPal</option>
        </select>
        <button type="submit" class="btn-search">Search</button>
    </form>

    {{#if mainItems}}
    <div class="search-result">
        <div class="search-list">
            {{#each mainItems}}
                <div class="coin">
                    <div class="coin-img">
                        <img src="{{image}}">
                    </div>
                    <div class="coin-info">
                        <h1>{{name}}</h1>
                        <p><span>Price: </span>${{price}}</p>
                        <p><span>Payment: </span>{{payment}}</p>
                    </div>

                    <a href="/details/{{_id}}/details" class="btn-details">Details</a>
                </div>
            {{/each}}
        </div>
    </div>
    {{else}}
     <div class="no-match">
        <p>No match was found!</p>
    </div>
    {{/if}}
    
</section>
  

  {{!-- <section class="crypto-market">
    <h1><span>Crypto</span> Trade</h1>
        {{#if mainItems}}
    <div class="offer-list">
                {{#each mainItems}}
                    {{> mainItem}}
                {{/each}}
    </div>
        {{else}}
    <div class="no-offer">
        <p>There are no crypto offers found!</p>
    </div> 
        {{/if}}
</section> --}}