

<main class="search">
    <section id="search">
        <h2>Search by Location</h2>

        <form class="search-wrapper cf" method="get">
            <input id="#search-input" type="text" name="location" placeholder="Search here..." value="{{searchLocation}}" required />
            <button type="submit">Search</button>
        </form>

        <div id="search-container">
            {{#if mainItems}}
            <ul class="card-wrapper">
                <!-- Display a li with information about every post (if any)-->
                {{#each mainItems}}
                    {{> mainItem}}
                {{/each}}
                {{!-- <li class="card">
                    <img src="./static/images/animal3.jpg"/>
                    <p>
                        <strong>Name: </strong><span class="name">Mama and Baby Lolly</span>
                    </p>
                    <p>
                        <strong>Location: </strong><span class="location">Toronto Zoo</span>
                    </p>
                    <p><strong>Need of:</strong><span class="need-of">Food</span></p>
                    <a class="details-btn" href="#">Details</a>
                </li> --}}
            </ul>
            {{else}}
            <!-- Display an h2 if there are no posts -->
            <div id="no-post">
                <h2>There are no results found.</h2>
            </div>
            {{/if}}
        </div>
    </section>
</main>


    